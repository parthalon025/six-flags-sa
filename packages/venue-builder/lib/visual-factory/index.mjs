/**
 * Visual factory — display compile and certification interface.
 *
 * Cross-module entry: compileDisplay. Truth reads go through map-io only.
 */

export { compileDisplay } from './compile-display.mjs';
export { displayDir, publishedDisplayDir } from './visual-io.mjs';
