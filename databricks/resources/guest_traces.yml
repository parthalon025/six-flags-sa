# Guest trace clustering → walkway candidates sidecar (serverless)
resources:
  jobs:
    parkbound_guest_traces:
      name: parkbound-guest-traces-${bundle.target}
      schedule:
        quartz_cron_expression: "0 30 6 * * ?"
        timezone_id: UTC
      environments:
        - environment_key: default
          spec:
            environment_version: "2"
      tasks:
        - task_key: cluster_traces
          environment_key: default
          spark_python_task:
            python_file: ../src/parkbound_job.py
            parameters:
              - --module
              - transform.guest_trace_cluster
              - --catalog
              - ${var.catalog}
              - --venue
              - kings-island
              - --gap-m
              - "12"
        - task_key: export_sidecar
          depends_on:
            - task_key: cluster_traces
          environment_key: default
          spark_python_task:
            python_file: ../src/parkbound_job.py
            parameters:
              - --module
              - export.write_sidecars
              - --catalog
              - ${var.catalog}
              - --kind
              - guest_traces
              - --venue
              - kings-island
